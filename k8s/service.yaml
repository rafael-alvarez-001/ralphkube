apiVersion: v1
kind: Service
metadata:
  name: flask-app-service
  labels:
    app: flask-app
spec:
  type: NodePort
  ports:
  - port: 5050
    targetPort: 5050
    nodePort: 30050
  selector:
    app: flask-app
